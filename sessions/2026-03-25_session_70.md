# Session Handover — 2026-03-25

## Last 5 commits
d92968b feat(bloque9): smart relevance scoring + fact extraction (batch mode)
9d0805b feat(bloque9): hybrid search engine — vector + FTS5 keyword + graph RRF
99f75fd feat(bloque9): connect vector_store to pipeline — cosine KNN replaces JSON cosine
9a78410 feat(bloque9): temporal memory + sqlite-vec migration
fc4e7f7 security: fail-closed auth + expanded prompt injection sanitization

## Uncommitted changes
(none)

## Tests
=========================== short test summary info ============================
FAILED tests/test_smoke.py::test_task_relevance_reranking - AssertionError: R...
1 failed, 37 passed in 4.56s

## Active services
active
active
inactive
active

## Next steps
- **PASO 1 — settings.json:** Añadir `MAX_THINKING_TOKENS=10000`, `CLAUDE_CODE_SUBAGENT_MODEL=haiku`, verificar `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=50`. Leer con Python (no Edit — el Shannon hook puede bloquearlo). NOTA: haiku afecta a TODOS los subagentes; si se detecta degradación en finance-specialist u orchestrator, revertir a sonnet.
- **PASO 2 — Instinct evolution:** Verificar tabla instincts (`PRAGMA table_info`, contar embeddings). Si no hay embeddings → clustering por campo `keyword`. Crear `bin/agents/instinct_evolver.py` (<100 líneas): query confidence>0.7, agrupar (keyword o cosine>0.8), grupos de 3+ → draft SKILL.md en `.claude/skills/evolved/`. Modos: `--report`, `--generate`, `--demo`. Cron semanal lunes 6am.
- **PASO 3 — Strategic compact hints:** Leer `.claude/hooks/postcompact.py`. Añadir al final: contar acciones de la sesión actual via `agent_actions` DB. Si >50 acciones → loguear hint de `/compact`; si >100 → warning. Heurístico pero útil.
- **PASO 4 — AgentShield (DESDE TERMINAL, no Claude Code):** Network disabled en sandbox CC. Ejecutar fuera: `cd /root/dqiii8 && npx ecc-agentshield scan . 2>&1 | tee /tmp/agentshield_scan.txt`. Dentro de CC: leer `/tmp/agentshield_scan.txt` si existe.
- **PASO 5 — MCP investigation (solo reportar, no instalar):** Verificar si están instalados: `@anthropic-ai/mcp-server-sequential-thinking` (razonamiento paso a paso, útil para orchestrator) y `context7` (docs de librerías, útil para python-specialist). Listar MCPs actuales en `.mcp.json`. Reportar valor potencial por MCP.
- **Fix `test_task_relevance_reranking`:** El re-ranking con `intent+entity` en `get_relevant_chunks()` no cambia el orden (todos los chunks tienen igual similarity). Diagnosticar y corregir la lógica de re-ranking o el test.
