[Service]
ExecStartPre=
ExecStartPre=-/usr/bin/fuser -k -TERM 8090/tcp
ExecStart=
ExecStart=/root/dqiii8/.venv-core/bin/python3 bin/ui/dashboard.py
# Same claude-CLI-path fix as dqiii8-bot's netcup-venv.conf — dashboard.py also
# shells out to "claude" (health checks, /api tiers). See that file's comment.
Environment="PATH=/root/.local/bin:/root/.bun/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
